# Codex Remote 控制台

双击 `Start-CodexRemotePWA.cmd` 打开本机控制台。该入口会运行项目根目录的 `CodexRemoteConsole.exe`，不依赖隐藏的 PowerShell 窗口。它不会注册任何 Windows 开机或登录自启动任务，关闭控制台也不会停止已经启动的 PWA 服务。

## PWA 服务

- 点击“启动服务”后，客户端在后台运行本地 Node 服务，并记录自己的进程号与日志到 `data/`。
- 点击“停止服务”只会停止该客户端启动并记录的服务；其他启动方式运行的服务不会被误停。
- “打开本机页面”访问本机回环地址；iPhone 应继续使用 Tailscale HTTPS 地址。
- “查看日志”用于排查服务不能启动、端口被占用或 Codex CLI 配置问题。

## Tailscale

- 启动或停止 Tailscale 会弹出 Windows 管理员确认，因为它操作的是系统服务。
- 停止 Tailscale 会断开 iPhone 的私网访问，但不会删除 tailnet 登录或 HTTPS Serve 配置。
- Tailscale 的自身服务由官方安装程序管理；本控制台不会创建或修改 Tailscale 的自启动设置。

## 设置

设置窗口可修改工作目录、端口、Tailscale HTTPS 地址、Codex 权限和本机历史显示。访问密码与 API Key 不会显示或由客户端修改。保存设置后，停止并重新启动 PWA 服务才能生效。

完整部署、Tailscale 和 iPhone 说明见 `教程.md`。

如需重新构建客户端，可运行 `scripts\\build-codex-remote-client.ps1`。客户端源码位于 `client\\CodexRemoteConsole.cs`。
